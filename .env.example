# Copy to .env.local for local dev, .env.production for production build.
# When unset, the app runs in static/demo mode — Excel upload disabled,
# no calls go to the backend.

VITE_API_URL=http://localhost:8000
